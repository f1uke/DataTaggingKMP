# Build Command

Build the DataTaggingKMP project for all platforms.

## Instructions

1. Run the Gradle build command:
   ```bash
   ./gradlew build
   ```

2. If the build succeeds, report success to the user.

3. If the build fails, analyze the error output and provide suggestions for fixing the issues.
